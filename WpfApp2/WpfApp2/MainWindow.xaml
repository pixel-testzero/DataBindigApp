<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="Демонстрация привязки данных" 
        Height="364" Width="622"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Двусторонняя привязка данных в WPF" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>


        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Введите текст:" Margin="0,0,0,5"/>
            <TextBox x:Name="InputTextBox" 
                     Text="{Binding TextValue, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                     Height="30"
                     FontSize="14"
                     Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Текущее значение в ViewModel:" Margin="0,0,0,5"/>
            <Border BorderBrush="Gray" 
                    BorderThickness="1" 
                    CornerRadius="3" 
                    Padding="5"
                    Background="#FFF0F0F0">
                <TextBlock Text="{Binding TextValue}" 
                           FontSize="14"
                           TextWrapping="Wrap"/>
            </Border>
        </StackPanel>


        <Button Grid.Row="3" 
                Content="Изменить через ViewModel" 
                Command="{Binding ChangeTextCommand}"
                Height="35"
                FontSize="14"
                HorizontalAlignment="Center"
                Width="200"/>
    </Grid>

</Window>
